# =============================================================================
# Ethereum Node - Reth + Lighthouse
# =============================================================================
# Copy this file to .env and customize for your deployment.
#
# Run as full node:    docker compose --profile full up -d
# Run as archive node: docker compose --profile archive up -d
# =============================================================================

# Data directory (mounted volume or local path)
DATA_DIR=/data

# Client versions (pin to specific versions in production)
# https://github.com/paradigmxyz/reth/releases
RETH_VERSION=v1.9.4
# https://github.com/sigp/lighthouse/releases
LIGHTHOUSE_VERSION=v8.0.0

# User/group IDs (match the host user that owns DATA_DIR)
UID=1000
GID=1000

# External IP for P2P discovery (required for cloud/VPS deployments)
# EXTERNAL_IP=YOUR_PUBLIC_IP
